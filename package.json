{
  "name": "grabi-web",
  "version": "1.0.0",
  "description": "Official website for Grabi — the no-drama free screen recorder for macOS",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler deploy"
  },
  "dependencies": {
    "astro": "^5.1.0",
    "@astrojs/sitemap": "^3.2.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
